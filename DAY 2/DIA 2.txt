// Solicita o nome do usuário e armazena na variável 'nome'
let nome = prompt("Qual o seu nome?");

// Solicita a idade do usuário e armazena na variável 'idade'
let idade = prompt("Quantos anos você tem?");

// Solicita a linguagem de programação que o usuário está estudando e armazena na variável 'linguagem'
let linguagem = prompt("Qual linguagem de programação você está estudando?");

// Exibe uma mensagem no console com as informações fornecidas pelo usuário
console.log(`Olá ${nome}, você tem ${idade} anos e já está aprendendo ${linguagem}!`);
